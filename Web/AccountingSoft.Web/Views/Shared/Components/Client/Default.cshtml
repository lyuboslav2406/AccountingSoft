@model AccountingSoft.Web.ViewModels.LayoutViewModel

@Html.DropDownList("Клиенти", @Model.Clients.Select(x => new SelectListItem() { Text = x.Name, Value = x.Id.ToString() }), "Избери Клиент", new { id="selectClient"})
